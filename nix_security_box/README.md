# Nix.SecurityBox

## Installation

The package can be installed by adding `nix_security_box` to your list of dependencies in `mix.exs`:

```elixir
def deps do
  [
    {:nix_security_box, github: "nicksen/nix_ex", subdir: "nix_security_box", depth: 1}
  ]
end
```
